<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Coins arrondis -->
    <corners android:radius="46dp" />

    <!-- Dégradé translucide avec trois couleurs -->
    <gradient
        android:angle="45"
        android:startColor="@color/colorPrimary"
        android:centerColor="#B8EFE9"
        android:type="linear" />

    <!-- Bordure légèrement transparente -->
    <stroke
        android:width="1dp"
        android:color="#50FFFFFF" />
</shape>